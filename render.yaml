services:
  # Python Flask Backend Web Service
  - type: web
    name: doomverse-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: TELEGRAM_API_ID
        sync: false
      - key: TELEGRAM_API_HASH
        sync: false
      - key: GROQ_API_KEY
        sync: false

  # React Static Site Frontend (100% Free, No Credit Card Required)
  - type: static_site
    name: doomverse-frontend
    buildCommand: npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: doomverse-backend
          property: host

